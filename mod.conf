name = bestguns
title = Best Guns
description = Standalone gun framework with a full firearm roster (fire/reload/scope). Self-contained: bundles its own controls + playerphysics fallbacks, so it has no hard dependencies.
optional_depends = controls, playerphysics
